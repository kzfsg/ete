{
	"version": 2,
	"names": {
		"ete-monorepo:": "Test Workspace Config",
		"folder:.other": "Test Workspace Config",
		"@ete/core:": "Core Tests",
		"folder:packages/core": "Core Tests",
		"ete-monorepo:fixtures": "Demo Fixture Server",
		"folder:fixtures": "Demo Fixture Server",
		"@ete/core:src @ete/core:test": "Core Tests",
		"@ete/driver-browser:src @ete/driver-browser:test": "Browser Automation Driver",
		"folder:packages/driver-browser": "Browser Automation Driver",
		"ete:": "CLI Tests",
		"folder:packages/cli": "CLI Tests",
		"ete:src ete:test": "CLI Tests",
		"ete:src ete:src/commands ete:test": "CLI Tests",
		"ete:src ete:src/commands ete:src/session ete:test": "CLI Tests",
		"@ete/site:lib @ete/site:test": "Site Store",
		"folder:packages/site": "Site Store",
		"@ete/site:app @ete/site:app/[owner]/[repo] @ete/site:app/[owner]/[repo]/[ref] @ete/site:app/[owner]/[repo]/[ref]/[run] @ete/site:lib @ete/site:test": "Site Store",
		"@ete/site:app @ete/site:app/[owner]/[repo] @ete/site:app/[owner]/[repo]/[ref] @ete/site:app/[owner]/[repo]/[ref]/[run] @ete/site:app/[owner]/[repo]/[ref]/[run]/manifest.json @ete/site:lib @ete/site:test": "Site Store"
	},
	"prose": {
		"ete-monorepo:": {
			"name": "Test Workspace Config",
			"kind": "config",
			"summary": "A workspace configuration layer, it tells the test runner how to assemble projects. It exists to organize test execution rather than implement product logic.\n\nProvides vitest.workspace.ts at the repository root.\n\nRuns at test tool startup.\n\nVitest reads it to discover workspace projects.",
			"role": "Handles repository test project wiring."
		},
		"folder:.other": {
			"name": "Test Workspace Config",
			"kind": "config",
			"summary": "A workspace configuration layer, it tells the test runner how to assemble projects. It exists to organize test execution rather than implement product logic.\n\nProvides vitest.workspace.ts at the repository root.\n\nRuns at test tool startup.\n\nVitest reads it to discover workspace projects.",
			"role": "Handles repository test project wiring."
		},
		"@ete/core:": {
			"name": "Core Tests",
			"kind": "library",
			"summary": "A unit test area, which contains assertions that check behavior in the core package.\n\nHolds packages/core/test/schema.test.ts, a schema focused test file under packages/core.\n\nruns under the test runner.\n\nRuns within Vitest, alongside the workspace config from g0.",
			"role": "Handles schema verification for the core package."
		},
		"folder:packages/core": {
			"name": "Core Tests",
			"kind": "library",
			"summary": "A domain core, it defines the shared data shapes and execution records for a testing workflow.\n\nDefines schemas and types for targets, steps, observations, anomalies, reports, run manifests, and helper parsing and hashing functions.\n\nPure library with no runtime of its own.\n\nUsed by the CLI, browser driver, and site. It underpins tests across the repo.",
			"role": "Responsible for test models, reporting, and run state."
		},
		"ete-monorepo:fixtures": {
			"name": "Demo Fixture Server",
			"kind": "service",
			"summary": "A fixture service, it stands in for an application under test. Fixtures like this let browser and core tests point at a stable target without depending on a real deployment.\n\nExports createDemoServer, root, and types from fixtures/demo-app/server.mjs.\n\nRuns when imported by tests.\n\nBrowser tests and core tests use it as their target app.",
			"role": "Serves a demo application for integration tests."
		},
		"folder:fixtures": {
			"name": "Demo Fixture Server",
			"kind": "service",
			"summary": "A fixture service, it stands in for an application under test. Fixtures like this let browser and core tests point at a stable target without depending on a real deployment.\n\nExports createDemoServer, root, and types from fixtures/demo-app/server.mjs.\n\nRuns when imported by tests.\n\nBrowser tests and core tests use it as their target app.",
			"role": "Serves a demo application for integration tests."
		},
		"@ete/core:src @ete/core:test": {
			"name": "Core Tests",
			"kind": "library",
			"summary": "A domain core, it defines the shared data shapes and execution records for a testing workflow.\n\nDefines schemas and types for targets, steps, observations, anomalies, reports, run manifests, and helper parsing and hashing functions.\n\nPure library with no runtime of its own.\n\nUsed by the CLI, browser driver, and site. It underpins tests across the repo.",
			"role": "Responsible for test models, reporting, and run state."
		},
		"@ete/driver-browser:src @ete/driver-browser:test": {
			"name": "Browser Automation Driver",
			"kind": "library",
			"summary": "A driver adapter, it wraps browser control behind a test execution interface.\n\nImplements BrowserDriver, locator helpers, telemetry methods, screenshots, and filmstrip assembly for browser based runs.\n\nLibrary used at runtime by the CLI.\n\nBuilds on the core engine. The CLI session and command code use it, and its tests exercise browser behavior.",
			"role": "Handles browser automation and filmstrip capture."
		},
		"folder:packages/driver-browser": {
			"name": "Browser Automation Driver",
			"kind": "library",
			"summary": "A driver adapter, it wraps browser control behind a test execution interface.\n\nImplements BrowserDriver, locator helpers, telemetry methods, screenshots, and filmstrip assembly for browser based runs.\n\nLibrary used at runtime by the CLI.\n\nBuilds on the core engine. The CLI session and command code use it, and its tests exercise browser behavior.",
			"role": "Handles browser automation and filmstrip capture."
		},
		"ete:": {
			"name": "CLI Tests",
			"kind": "library",
			"summary": "A test component is a collection of verification code that exercises another part of the system from the outside. This one only contains CLI test files and a port helper, so it is about checking the command line area rather than shipping the command line itself.\n\nHolds packages/cli/test/app.test.ts and config.test.ts, with the freePort helper used by those tests.\n\nTest code that runs under the test runner.\n\nUses the CLI test files and the freePort helper; stands alone.",
			"role": "Handles CLI behavior checks."
		},
		"folder:packages/cli": {
			"name": "CLI Tests",
			"kind": "gateway",
			"summary": "A command line entrypoint layer, it turns user commands and flags into app and session operations.\n\nDefines the top level program, app lifecycle helpers, config schema, and command entry points for run, session, publish, report, and init.\n\nRuns as a CLI process.\n\nUses the core engine and browser driver. The core tests and session helpers call it.",
			"role": "Handles command parsing, app startup, and config loading."
		},
		"ete:src ete:test": {
			"name": "CLI Tests",
			"kind": "library",
			"summary": "A command line support area, combining startup control with configuration parsing for the CLI package.\n\nProvides the app handle and startup helpers alongside config schema, parsing, and loading utilities.\n\nRuns at test time and as a process helper.\n\nThe app controller uses the config parser, and the CLI tests exercise both.",
			"role": "Handles app startup and config loading."
		},
		"ete:src ete:src/commands ete:test": {
			"name": "CLI Tests",
			"kind": "gateway",
			"summary": "A command line entry layer that prepares runtime configuration and exposes user facing commands. It sits above the core runner and browser driver and translates process inputs into library calls.\n\nProvides model creation, lazy resolver setup, app lifecycle helpers, config loading, the command program, and the run and report command implementations.\n\nRuns as a CLI process.\n\nCalls the core library for parsing and running tests, and uses the browser driver when commands need automation.",
			"role": "Handles command line config and commands."
		},
		"ete:src ete:src/commands ete:src/session ete:test": {
			"name": "CLI Tests",
			"kind": "gateway",
			"summary": "A command line entrypoint layer, it turns user commands and flags into app and session operations.\n\nDefines the top level program, app lifecycle helpers, config schema, and command entry points for run, session, publish, report, and init.\n\nRuns as a CLI process.\n\nUses the core engine and browser driver. The core tests and session helpers call it.",
			"role": "Handles command parsing, app startup, and config loading."
		},
		"@ete/site:lib @ete/site:test": {
			"name": "Site Store",
			"kind": "library",
			"summary": "A small data access layer for listing stored runs and refs. It turns repository folders into summaries that a site can render.\n\nDefines listFolders, listRuns, runPrefixOf, refLabel, and related helpers in packages/site/lib/store.ts.\n\npure library with no runtime of its own.\n\nBuilds on core run summary types and is tested by the site store suite.",
			"role": "Handles listing stored runs and refs."
		},
		"folder:packages/site": {
			"name": "Site Store",
			"kind": "ui",
			"summary": "A web route layer, it maps URL paths to HTML and JSON responses for stored runs.\n\nDefines site routes, layout, storage helpers, HTML rendering helpers, and manifest and run listing endpoints.\n\nRuns as a web app.\n\nBuilds on the core engine. Its route handlers and library helpers are used by the site pages and tests.",
			"role": "Serves repository run pages and manifests."
		},
		"@ete/site:app @ete/site:app/[owner]/[repo] @ete/site:app/[owner]/[repo]/[ref] @ete/site:app/[owner]/[repo]/[ref]/[run] @ete/site:lib @ete/site:test": {
			"name": "Site Store",
			"kind": "ui",
			"summary": "A server rendered site layer with dynamic routes and shared HTML helpers. It reads stored run data and turns it into listing and detail pages.\n\nProvides the root layout, repository and ref routes, run detail pages, storage listing helpers, HTML rendering helpers, and blob access helpers.\n\nserver routes called per request.\n\nBuilds on the core models and the site library helpers. The site tests verify the storage helpers, and the route handlers use them to render pages.",
			"role": "Serves repository and run pages."
		},
		"@ete/site:app @ete/site:app/[owner]/[repo] @ete/site:app/[owner]/[repo]/[ref] @ete/site:app/[owner]/[repo]/[ref]/[run] @ete/site:app/[owner]/[repo]/[ref]/[run]/manifest.json @ete/site:lib @ete/site:test": {
			"name": "Site Store",
			"kind": "ui",
			"summary": "A web route layer, it maps URL paths to HTML and JSON responses for stored runs.\n\nDefines site routes, layout, storage helpers, HTML rendering helpers, and manifest and run listing endpoints.\n\nRuns as a web app.\n\nBuilds on the core engine. Its route handlers and library helpers are used by the site pages and tests.",
			"role": "Serves repository run pages and manifests."
		}
	}
}