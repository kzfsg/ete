# ete Phase 4 Implementation Plan
Spec: `docs/superpowers/specs/2026-09-14-ete-phase4-central-site-design.md`

1. core: export `renderHtmlWith(reports, asset, title)`; manifest type `RunManifest`.
2. cli: `ete publish` (upload via `@vercel/blob`, manifest, prune) with tests against a fake blob client.
3. site: Next.js app `packages/site` with the four routes + tests for the listing/model helpers.
4. action: replace deploy/publish steps with `ete publish`; inputs `blob-token`, `site-url`; comment uses run URL.
5. Deploy site; wire secrets on both PRs; prove end to end.
