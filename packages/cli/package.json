{
  "name": "ete",
  "version": "0.0.1",
  "description": "LLM-authored, deterministically replayed E2E tests for CI",
  "type": "module",
  "bin": {
    "ete": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json && chmod +x dist/index.js && mkdir -p dist/map && cp src/map/canvas.html dist/map/",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ete/core": "workspace:*",
    "@ete/driver-browser": "workspace:*",
    "@vercel/blob": "^2.8.0",
    "commander": "^14.0.0",
    "fast-glob": "^3.3.3",
    "yaml": "^2.8.2",
    "zod": "^4.6.4"
  }
}
